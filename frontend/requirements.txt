# --- Reflex UI framework (pinned: app was written against 0.7.x) ---
reflex==0.7.9
sqlmodel==0.0.24
# sqlmodel 0.0.24 breaks on pydantic>=2.12; keep it in range.
pydantic>=2.10,<2.12

# --- messaging / backend calls ---
aio-pika==9.6.2
httpx>=0.27

# --- auth ---
PyJWT>=2.9
passlib==1.7.4
bcrypt==4.0.1            # passlib 1.7.4 is incompatible with bcrypt>=4.1
psycopg2-binary>=2.9    # PostgreSQL RBAC (sharing); optional at runtime

# --- data / plots used by the chat rendering ---
pandas>=2.0
numpy>=1.26
plotly>=5.0
pillow>=10.0

# --- misc ---
python-dotenv>=1.0
openai>=1.0             # imported by state.py; not used by the mock flow

# used by the original state.py for its internal API calls
requests>=2.31
